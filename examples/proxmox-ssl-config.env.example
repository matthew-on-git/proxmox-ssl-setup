# Proxmox SSL Configuration Example
# Copy this file and update with your values

# Proxmox Configuration
PROXMOX_DOMAIN="proxmox.example.com"
PROXMOX_EMAIL="admin@example.com"
PROXMOX_TYPE="ve"  # or "pbs" for Proxmox Backup Server

# Cloudflare Configuration
CF_TOKEN="your_cloudflare_api_token_here"

# Optional: Custom CA bundle path
# CA_BUNDLE="/path/to/ca-cert.pem"

# Optional: Disable SSL verification (not recommended)
# NO_VERIFY_SSL="true"
